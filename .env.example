# Google Gemini API key. Add this in Vercel Project Settings > Environment Variables.
GEMINI_API_KEY=

# Optional: owner/admin password. If omitted, the app currently uses its built-in fallback.
OWNER_PASSWORD=
